let values = [1, 2, 3, 44];

// values.pop(); // removes last value in the array

// console.log(values );

// let result = values.shift() // removes the first value and moves everything else down one index

// console.log(result, values );

// values.splice(0, 3); // the first number will be the index I want to delete
// the second number is how many values I want to delete in the index including the index selected

 console.log(values.length) // returns the number of the length of the array

